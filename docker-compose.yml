version: '3'

services:
  web: 
    command: python3 app.py
    image: taetakdanai/a3_car_price:a3_1
    build: 
      context: .
      dockerfile: .Dockerfile
    platform: linux/amd64
    environment:
      - HOST=0.0.0.0
      - PORT=80
    ports:
      - 4000:80